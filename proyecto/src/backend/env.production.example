# Configuración de Producción - StudentsPoint
# Copiar este archivo como .env y configurar las variables

# ---- Seguridad ----
SECRET_KEY=tu-clave-secreta-super-segura-aqui
DEBUG=0
ALLOWED_HOSTS=studentspoint.app,www.studentspoint.app,tu-dominio.com

# ---- Base de Datos PostgreSQL ----
DB_ENGINE=django.db.backends.postgresql
DB_NAME=studentspoint_prod
DB_USER=postgres
DB_PASSWORD=214526867
DB_HOST=localhost
DB_PORT=5432

# ---- CORS ----
CORS_ALLOWED_ORIGINS=https://studentspoint.app,https://www.studentspoint.app

# ---- Google OAuth ----
GOOGLE_REDIRECT_URI=https://studentspoint.app/api/auth/google/callback/web/
FRONTEND_URL=https://studentspoint.app

# ---- Celery (Redis) ----
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ---- Email (Opcional) ----
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=admin@studentspoint.app
EMAIL_HOST_PASSWORD=tu-password-de-email

# ---- Archivos Estáticos ----
STATIC_ROOT=/var/www/studentspoint/staticfiles/
MEDIA_ROOT=/var/www/studentspoint/media/

